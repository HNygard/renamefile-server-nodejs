Throw together Javascript code to just get the job done.

Two modes:
- Rename with one input field
- JSON Creator ("Categorizer") that have buttons for accounting, payment type, etc

Install node packages:
    npm install

Make the app accessible from command line:
    ln -s `pwd`/renamefile-server-nodejs.sh ~/bin/renamefile-server

Go into any directory in your terminal and start the app:
    renamefile-server
